// Google OAuth removed — keep file as a placeholder for future strategies
const passport = require("passport");

// No strategies registered. If you add other strategies later, register them here.

module.exports = passport;
